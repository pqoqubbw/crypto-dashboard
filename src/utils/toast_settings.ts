import { ToastOptions } from 'react-toastify';

export const toast_settings = {
  position: 'top-right',
  autoClose: 5000,
  hideProgressBar: true,
  closeOnClick: true,
  pauseOnHover: true,
  draggable: true,
  progress: 0,
} as ToastOptions;
